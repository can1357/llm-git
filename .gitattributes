* text=auto eol=lf

# Force LF for embedded prompt templates so the USER separator parses
# identically on Windows checkouts (Git's default core.autocrlf=true).
lgit/resources/prompts/**/*.md text eol=lf
*.toml text eol=lf
